This is actually a library! This is a tshark inspector.

Need to change match protocol. The protocol ports should be a parameter when reading the events.
{8000 :dynamodb 11211 :memcache}

I need a way to identify a protocol
I might split a single message into multiple ones (memcache as vector), or viceversa (postgres grouping of multiple messages).
Is the above a transducer?
I offer the range macro to register tags plus timestamps that will be dumped next to my log file when they happen.
I offer a way to register a pid with a tag so that I listen to ports from those pids to know later how to tag ports and dumping to a file
next to my log file.


My clients can register their decoders per protocol.
First they'd need to have a way to identify the protocol they understand.
The protocol is matched on, it should match the ports for one that's owned.

Should protocols be composable? In case of dynamo over http. So I can have an http protocol, then parse the http with dynamo,
then dynamo with datomic specifics, how should we do this?

Multiple pass? Or I guess we can add a layer! So that in a second pass over protocols. But then I should either pass those messages
to the subprotocol.

For protocols being composables, then.... I'm not sure. Because we'd have the request/response filtering, how could that work?

Per sub protocol, it's transducer can decide that it needs the next message (same stream) to send a message, and keep some in memory.




